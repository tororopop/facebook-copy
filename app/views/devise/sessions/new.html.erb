
<div class="row">
  <div class="col-xs-8 col-xs-offset-1 col-sm-5">

    <img src="<%= asset_path "facebooktop2.png" %>" style="padding-top: 70px;">

    <p style="font-size: 20px; font-weight: bold; padding-top: 10px;">
      Facebookへようこそ！「いいね！」で広がる世界
    </p>
    <p style="font-size: 16px; text-align: center; padding-top: 10px;">
      Facebookで大切な友達とつながろう。
    </p>


  </div>

  <div class="col-xs-9 col-xs-offset-1 col-sm-4 col-sm-offset-1">
    <h1 style="font-size: 30px; font-weight: bold; padding-top: 20px;">
      ログイン
    </h1>

    <p>情報のプライバシーは設定で管理できるので安心です。</p>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

      <div class="field">
        <%= f.email_field :email, autofocus: true, class: "form-control", placeholder: "メールアドレス" %>
      </div>

      <div class="field">
        <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "パスワード" %>
      </div>

      <% if devise_mapping.rememberable? %>
        <div class="field">
          <%= f.check_box :remember_me %>
          <%= f.label :内容を記憶させる %>
        </div>
      <% end %>

      <div class="actions">
        <%= f.submit "ログイン", class: "btn btn-primary btn-block btn-lg" %>
      </div>

    <% end %>

    <%= render "devise/shared/links" %>

  </div>
</div>
